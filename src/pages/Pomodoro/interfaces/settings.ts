export type settingsType = 'defaultConfig' | 'longConfig' | 'customConfig';

export interface ISettingsValues {
    workTimerValue: number;
    restTimerValue: number;
    longRestTimerValue: number;
}
